{
  "name": "shpp_level_2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "start": "node dist/index.js",
    "task": "npm run build && node dist/$npm_config_folder/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.4.0",
    "rimraf": "^6.1.3"
  }
}
